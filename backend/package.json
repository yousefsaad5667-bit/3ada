{
  "name": "habit-tracker-api",
  "version": "1.0.0",
  "description": "Backend API for Habit Tracker",
  "scripts": {
    "dev": "wrangler dev",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 execute habit-tracker-db --local",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240101.0",
    "drizzle-kit": "^0.21.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
